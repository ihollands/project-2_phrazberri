<!DOCTYPE html>
<html>
  <head>
    <title>Phrazberri</title>
    <link href="https://fonts.googleapis.com/css?family=Alef|Pacifico|PT+Mono|Permanent+Marker|Quicksand|Scheherazade" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      <% if !current_user %>
      <header>
        <h1 class="title">Phrazberri</h1>
        <h3>Because knowing idioms is <span id="berry">berry</span> important!</h3>
          <div class="top">
          <%= link_to "Sign up", new_user_registration_path, class: "user_link" %>
          <%= link_to "Sign in", new_user_session_path, class: "user_link" %>
          </div>
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
      </header>
      <% else %>
      <header>
        <a href="/languages"  class="title" id="title_link">Phrazberri [home]</a>
        <p>Signed in as <span><%= current_user.email %></span> | <%= link_to "Sign out", destroy_user_session_path, :method => :delete, class: "user_link" %></p>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </header>
      <% end %>

      <!-- AM: Noticing all of your views are wrapped in a div with a class of "inside". -->
      <!-- AM: If it doesn't mess up your app, why not just wrap the below yield statement in that "inside"div. That way you don't have to write that line of code in each of your views. -->
      <%= yield %>



  </body>
</html>
